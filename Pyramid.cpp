#include "Pyramid.hpp"

// The pyramid is not a square 2D matrix, it is a pyramid: the row lists are shorter at the top and get longer as they go down.
void Pyramid::initPyramid(std::string fileloc) {
	
}
